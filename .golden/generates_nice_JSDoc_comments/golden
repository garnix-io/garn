import { mkPackage } from "../../ts/base.ts";

/**
 * This one - eh, not so good!
 */
export const bar = mkPackage({
  attribute: `pkgs.bar`,
});

/**
 * This is the bestest derivation.
 */
export const foo = mkPackage({
  attribute: `pkgs.foo`,
});
