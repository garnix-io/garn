import { mkOldPackage } from "../../ts/base.ts";

/**
 * This one - eh, not so good!
 */
export const bar = mkOldPackage({
  expression: `pkgs.bar`,
  description: "This one - eh, not so good!"
});

/**
 * This is the bestest derivation.
 */
export const foo = mkOldPackage({
  expression: `pkgs.foo`,
  description: "This is the bestest derivation."
});
