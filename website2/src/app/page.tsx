import { Header } from "@/components/header";
import { Hero } from "@/components/hero";

const Home = () => {
  return (
    <main>
      <Header />
      <Hero />
    </main>
  );
};

export default Home;
